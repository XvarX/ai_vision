# 🚀 AI Vision 部署中心

欢迎使用 AI Vision 部署中心！本文档提供所有部署场景的完整指引。

## 📑 快速导航

### 新手推荐路径

```
1. 开始阅读 [部署场景选择](#-部署场景选择) 找到适合你的方案
2. 阅读对应场景的详细指南
3. 按照步骤操作，完成部署
4. 参考 [最佳实践](#-最佳实践) 优化配置
```

---

## 🎯 部署场景选择

### 回答 3 个问题，快速找到最适合的方案

#### 问题 1：你的用户主要在哪里？

- **🇨🇳 国内为主** → 推荐方案 A 或 B
- **🌍 国外为主** → 推荐方案 C
- **🌐 全球分布** → 推荐方案 D

#### 问题 2：你的技术水平和团队规模？

- **👤 1-2 人（个人/小团队）** → 推荐方案 A 或 C
- **👥 3-10 人（中型团队）** → 推荐方案 B 或 C
- **🏢 10+ 人（大型团队）** → 推荐方案 D

#### 问题 3：你的预算范围？

- **💰 免费/低成本（¥0-200/月）** → 推荐方案 A 或 C
- **💵 中等预算（¥200-500/月）** → 推荐方案 B
- **💎 充足预算（¥500+/月）** → 推荐方案 D

---

## 📋 部署方案一览

| 方案 | 描述 | 前端 | 后端 | 难度 | 月成本 | 推荐场景 |
|------|------|------|------|------|--------|---------|
| **A. Docker 全部** | 一键部署，单机运行 | Docker | Docker | ⭐⭐ | ¥50-200 | 国内用户、小团队、低成本 |
| **B. Vercel + 自建** | 前端云平台，后端自管 | Vercel | Docker | ⭐⭐⭐ | ¥100-300 | 需要前端 CDN、后端可控 |
| **C. Vercel + Railway** | 全托管，零运维 | Vercel | Railway | ⭐ | $20-50 | 国外用户、快速上线 |
| **D. K8s 全部** | 企业级，完全控制 | K8s | K8s | ⭐⭐⭐⭐ | $200-500+ | 大流量、高可用、企业级 |

---

## 📖 详细部署指南

### 方案 A：Docker 全部（推荐新手）

**适合场景**：
- ✅ 国内用户为主
- ✅ 个人项目或小团队
- ✅ 预算有限（单台服务器）
- ✅ 需要快速上线

**特点**：
- ✅ 配置简单，一条命令启动
- ✅ 前后端在一台服务器
- ✅ 成本最低（~¥50-100/月）
- ⚠️ 扩展性有限

**快速开始**：
```bash
# 1. 修改环境变量
cp .env.example .env
nano .env

# 2. 启动所有服务
docker-compose up -d

# 3. 查看状态
docker-compose ps
```

**详细指南**：[DEPLOYMENT.md - Docker 部署](DEPLOYMENT.md#docker-部署推荐)

**配置文件**：
- [docker-compose.yml](../docker-compose.yml) - 服务编排
- [nginx.conf](../nginx.conf) - 反向代理
- [.env.example](../.env.example) - 环境变量模板

---

### 方案 B：Vercel 前端 + Docker 后端

**适合场景**：
- ✅ 需要前端全球加速
- ✅ 后端需要完全控制
- ✅ 有一定运维能力

**特点**：
- ✅ 前端自动 CDN 加速
- ✅ 后端完全可控
- ✅ 灵活性高
- ⚠️ 需要自己管理后端服务器

**架构**：
```
用户 → Vercel CDN → 前端
              ↓
       HTTPS 请求
              ↓
         你的 Nginx → Docker 后端
```

**快速开始**：
```bash
# === 后端部署 ===
# 1. 构建并运行后端
cd backend
docker build -t ai-vision-backend .
docker run -d -p 8000:8000 --name backend ai-vision-backend

# 2. 配置 Nginx 反向代理（参考 DOCKER_STANDALONE.md）

# === 前端部署 ===
# 3. 安装 Vercel CLI
npm install -g vercel

# 4. 部署前端
cd frontend
vercel

# 5. 配置环境变量
# 在 Vercel 控制台设置: NEXT_PUBLIC_API_URL=https://api.yourdomain.com
```

**详细指南**：
- 后端：[DOCKER_STANDALONE.md](DOCKER_STANDALONE.md)
- 前端：[vercel.md](vercel.md)

**配置文件**：
- [backend/Dockerfile](../backend/Dockerfile) - 后端镜像
- [frontend/vercel.json](../frontend/vercel.json) - Vercel 配置

---

### 方案 C：Vercel 前端 + Railway 后端（最简单）

**适合场景**：
- ✅ 国外用户为主
- ✅ 不想管理服务器
- ✅ 快速上线 MVP
- ✅ 个人项目

**特点**：
- ✅ 零运维，全自动
- ✅ 自动扩展
- ✅ 部署最简单
- ✅ 免费额度够用
- ⚠️ 国内访问较慢

**快速开始**：
```bash
# === 后端部署到 Railway ===
# 1. 安装 Railway CLI
npm install -g railway

# 2. 登录并初始化
railway login
railway init

# 3. 部署后端
railway up

# 4. 获取后端 URL
railway domain

# === 前端部署到 Vercel ===
# 5. 连接 GitHub 并部署到 Vercel
# 访问 vercel.com 导入项目

# 6. 配置环境变量
# NEXT_PUBLIC_API_URL=https://your-backend.railway.app
```

**详细指南**：[vercel.md](vercel.md#部署到-railway)

**配置文件**：
- [frontend/vercel.json](../frontend/vercel.json) - Vercel 配置
- Railway 会自动检测 [backend/Dockerfile](../backend/Dockerfile)

---

### 方案 D：Kubernetes 全部（企业级）

**适合场景**：
- ✅ 大流量（10000+ 日活）
- ✅ 需要高可用
- ✅ 有运维团队
- ✅ 需要完全控制

**特点**：
- ✅ 完全控制
- ✅ 自动扩展
- ✅ 高可用
- ✅ 企业级
- ⚠️ 复杂度高，需要专业知识

**架构**：
```
用户 → Ingress (Nginx) → 前端 Service → 前端 Pod
              ↓
         后端 Service → 后端 Pod (多副本)
              ↓
         PostgreSQL Service → 数据库 Pod
```

**快速开始**：
```bash
# 1. 安装 kubectl 并连接集群

# 2. 构建并推送镜像
docker build -t your-registry/ai-vision-backend:latest backend
docker push your-registry/ai-vision-backend:latest

# 3. 修改配置文件
# - k8s/secrets.yaml: 修改密钥
# - k8s/backend-deployment.yaml: 修改镜像地址
# - k8s/frontend-deployment.yaml: 修改镜像地址

# 4. 部署
kubectl apply -f k8s/configmap.yaml
kubectl apply -f k8s/secrets.yaml
kubectl apply -f k8s/postgres-deployment.yaml
kubectl apply -f k8s/backend-deployment.yaml
kubectl apply -f k8s/frontend-deployment.yaml

# 5. 查看状态
kubectl get pods
kubectl get services
```

**详细指南**：[k8s.md](k8s.md)

**配置文件**：
- [k8s/frontend-deployment.yaml](../k8s/frontend-deployment.yaml) - 前端 K8s 配置
- [k8s/backend-deployment.yaml](../k8s/backend-deployment.yaml) - 后端 K8s 配置
- [k8s/postgres-deployment.yaml](../k8s/postgres-deployment.yaml) - 数据库 K8s 配置
- [k8s/secrets.yaml](../k8s/secrets.yaml) - 密钥配置（不提交到 Git）
- [k8s/configmap.yaml](../k8s/configmap.yaml) - 配置项

---

## 🔧 特殊场景

### 仅后端部署（已有前端）

如果只有后端需要部署：

**单 Docker**：[DOCKER_STANDALONE.md](DOCKER_STANDALONE.md)
```bash
docker run -d -p 8000:8000 ai-vision-backend:latest
```

**K8s**：[k8s/backend-deployment.yaml](../k8s/backend-deployment.yaml)
```bash
kubectl apply -f k8s/backend-deployment.yaml
```

### 仅前端部署（已有后端）

如果只有前端需要部署：

**Vercel**：[vercel.md](vercel.md)
```bash
vercel
```

**Docker**：参考 [DEPLOYMENT.md](DEPLOYMENT.md)

### 本地生产环境

在本地服务器运行：

**传统方式**：[DEPLOYMENT.md - 传统服务器部署](DEPLOYMENT.md#传统服务器部署)
```bash
# 使用 Gunicorn + PM2
gunicorn app.main:app -c gunicorn.conf.py
```

---

## 📚 完整文档索引

### 核心文档

| 文档 | 用途 | 适合人群 |
|------|------|---------|
| **[README.md](README.md)** | 项目介绍和快速开始 | 所有人 |
| **[DEPLOYMENT.md](DEPLOYMENT.md)** | 传统部署详细指南 | 需要完整了解 |
| **[DEPLOYMENT_SCENARIOS.md](DEPLOYMENT_SCENARIOS.md)** | 所有场景对照表 | 决策参考 |
| **本文档** | 部署入口和导航 | **首次部署必读** |

### Docker 相关

| 文档 | 内容 |
|------|------|
| **[docker-compose.yml](docker-compose.yml)** | Docker 编排配置 |
| **[nginx.conf](nginx.conf)** | Nginx 反向代理配置 |
| **[backend/Dockerfile](backend/Dockerfile)** | 后端镜像构建 |
| **[frontend/Dockerfile](frontend/Dockerfile)** | 前端镜像构建 |
| **[DOCKER_STANDALONE.md](DOCKER_STANDALONE.md)** | Docker 单独部署指南 |

### Kubernetes 相关

| 文档 | 内容 |
|------|------|
| **[k8s/README.md](k8s/README.md)** | K8s 部署完整指南 |
| **[k8s/frontend-deployment.yaml](k8s/frontend-deployment.yaml)** | 前端 K8s 配置 |
| **[k8s/backend-deployment.yaml](k8s/backend-deployment.yaml)** | 后端 K8s 配置 |
| **[k8s/postgres-deployment.yaml](k8s/postgres-deployment.yaml)** | 数据库 K8s 配置 |
| **[k8s/secrets.yaml](k8s/secrets.yaml)** | 密钥配置 |
| **[k8s/configmap.yaml](k8s/configmap.yaml)** | 配置项 |

### Vercel 相关

| 文档 | 内容 |
|------|------|
| **[frontend/vercel.json](frontend/vercel.json)** | Vercel 配置 |
| **[frontend/vercel-deployment-guide.md](frontend/vercel-deployment-guide.md)** | Vercel 完整指南 |

### 其他配置

| 文件 | 用途 |
|------|------|
| **[backend/gunicorn.conf.py](backend/gunicorn.conf.py)** | 生产后端配置 |
| **[backend/requirements-prod.txt](backend/requirements-prod.txt)** | 生产依赖 |
| **[.env.example](.env.example)** | 环境变量模板 |

---

## ✅ 部署检查清单

### 部署前准备

- [ ] 已阅读并选择了部署方案
- [ ] 已准备好服务器或云平台账号
- [ ] 已安装必要的工具（Docker/kubectl/Vercel CLI）
- [ ] 已准备好域名（可选）

### 安全配置（必做）

- [ ] 修改所有默认密钥和密码
- [ ] 配置强随机 SECRET_KEY
- [ ] 设置数据库强密码
- [ ] 配置防火墙规则
- [ ] 启用 HTTPS
- [ ] 配置 CORS 白名单

### 功能测试

- [ ] 前端页面正常访问
- [ ] 后端 API 正常响应
- [ ] 用户注册登录功能正常
- [ ] 数据库连接正常
- [ ] 所有核心功能可用

### 监控和日志

- [ ] 配置日志收集
- [ ] 设置错误监控（Sentry 等）
- [ ] 配置备份策略
- [ ] 设置告警通知

---

## 💡 最佳实践

### 安全

1. **永远不要提交密钥到 Git**
   ```bash
   # .gitignore 已包含
   k8s/secrets.yaml
   .env
   .env.local
   ```

2. **生成强密钥**
   ```bash
   # 生成 32 位随机密钥
   python -c "import secrets; print(secrets.token_urlsafe(32))"
   ```

3. **使用环境变量管理密钥**
   - Docker: 使用 `.env` 文件或 Docker Secrets
   - K8s: 使用 Secrets
   - Vercel: 使用环境变量

### 性能优化

1. **前端优化**
   - 启用 CDN（Vercel 自动提供）
   - 优化图片（Next.js Image）
   - 代码分割

2. **后端优化**
   - 使用连接池
   - 添加 Redis 缓存
   - 数据库索引优化

3. **数据库优化**
   - 生产环境使用 PostgreSQL
   - 定期备份
   - 监控慢查询

### 监控

1. **应用监控**
   - Sentry（错误追踪）
   - Vercel Analytics（前端性能）
   - Prometheus + Grafana（K8s）

2. **日志收集**
   - Docker logs
   - K8s logs
   - ELK Stack

---

## 🆘 故障排查

### 常见问题

| 问题 | 可能原因 | 解决方案 |
|------|---------|---------|
| **容器无法启动** | 端口占用/配置错误 | `docker logs` 查看日志 |
| **API 请求失败** | CORS/网络问题 | 检查后端 CORS 配置 |
| **数据库连接失败** | 连接字符串错误 | 验证 DATABASE_URL |
| **前端环境变量无效** | 变量名错误 | 确保以 `NEXT_PUBLIC_` 开头 |
| **K8s Pod 无法启动** | 镜像拉取失败/资源不足 | `kubectl describe pod` 查看 |

### 获取帮助

1. **查看对应部署指南的故障排查章节**
2. **查看日志**：
   - Docker: `docker logs -f <container>`
   - K8s: `kubectl logs -f <pod>`
   - Vercel: 控制台查看
3. **提交 GitHub Issue**

---

## 🔄 迁移指南

### 从本地迁移到 Docker

1. 构建 Docker 镜像
2. 导出数据库数据
3. 启动 Docker 容器
4. 导入数据
5. 切换 DNS

### 从 Docker 迁移到 K8s

1. 推送镜像到仓库
2. 创建 K8s 配置
3. 部署到 K8s
4. 验证功能
5. 切换流量

### 从自建迁移到云平台

参考对应云平台的迁移文档。

---

## 💰 成本对比

| 方案 | 月成本 | 说明 |
|------|--------|------|
| **Docker 单机** | ¥50-200 | 1-2 核服务器 |
| **Vercel + Docker** | ¥100-300 | Vercel 免费额度 + 服务器 |
| **Vercel + Railway** | $20-50 | 按使用量付费 |
| **K8s 全部** | $200-500+ | 取决于节点数量 |

---

## 📞 支持

- 📖 [完整文档](#-完整文档索引)
- 🐛 [提交 Issue](https://github.com/yourusername/ai_vision/issues)
- 💬 [讨论区](https://github.com/yourusername/ai_vision/discussions)

---

## 🎉 开始部署

**准备好了吗？选择一个方案开始部署：**

- 新手或国内用户 → [方案 A: Docker 全部](#-方案-a-docker-全部推荐新手)
- 国外用户快速上线 → [方案 C: Vercel + Railway](#-方案-c-vercel-前端--railway-后端最简单)
- 企业级应用 → [方案 D: K8s 全部](#-方案-d-kubernetes-全部企业级)

祝部署顺利！🚀
