# Docker Compose 环境变量配置示例
# 使用方法：复制此文件为 .env 并修改配置
# cp deployconfig/docker/.env.docker.example deployconfig/docker/.env

# ================================
# PostgreSQL 数据库配置
# ================================
POSTGRES_USER=aivision
POSTGRES_PASSWORD=changeme  # ⚠️ 部署时必须修改为强密码
POSTGRES_DB=aivision
POSTGRES_PORT=5432  # 数据库端口映射到宿主机

# ================================
# FastAPI 后端配置
# ================================
# JWT 密钥（至少 32 位随机字符）
# 生成方法: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=changeme  # ⚠️ 部署时必须修改

# JWT 算法
ALGORITHM=HS256

# Token 过期时间（分钟）
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# ================================
# 前端配置
# ================================
# 后端 API 地址（浏览器访问地址）
NEXT_PUBLIC_API_URL=http://localhost:8080

# ================================
# Nginx 端口配置
# ================================
# 前端访问端口（HTTP）
NGINX_HTTP_PORT=80

# 后端 API 访问端口
NGINX_API_PORT=8080

# HTTPS 端口（可选）
NGINX_HTTPS_PORT=443
